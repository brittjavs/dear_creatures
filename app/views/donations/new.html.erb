<div class="page">
    <div class="form">
        <h2>Donation Form</h2>
        <%if flash[:error]%>
        <%=flash[:error]%>
        <%end%>
        
        <%if flash[:warning]%>
        <%=flash[:warning]%>
        <%end%>
        <br>
        <%=form_for @donation do |f|%>
        <%=f.label :shelter_id, "Select a Shelter/Rescue to Donate to"%><br>
        <%=f.collection_select :shelter_id, Shelter.all, :id, :name, :include_blank => true%><br>
        <%=f.label :item_id, "Select the item you would like to donate or select 'Funding' from the dropdown"%><br>
        <%=f.collection_select :item_id, Item.all, :id, :name, :include_blank => true%><br>
        If you are donating an item, please enter a quantity.<br>
        <%=f.label :quantity%>
        <%=f.number_field :quantity, min: 1%><br>
        If donating funds, please enter a dollar amount.<br>
        <%=f.label :dollar_amount%>
        <%=f.number_field :dollar_amount, step: "any", min: 1%>
        <br>
        <%=f.submit "Submit Donation", class: "button"%>
        <%end%>
    </div>
</div>